import { BsStarFill } from "react-icons/bs";
import { IconBaseProps } from "react-icons/lib";

const StarIcon = (props: IconBaseProps) => {
  return <BsStarFill {...props} />;
};

export default StarIcon;
