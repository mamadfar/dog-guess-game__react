import { BsFillSuitHeartFill } from "react-icons/bs";
import { IconBaseProps } from "react-icons/lib";

const HeartIcon = (props: IconBaseProps) => {
  return <BsFillSuitHeartFill {...props} />;
};

export default HeartIcon;
